eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

zuul:
  routes:
    agency:
      path: /agency/**
      service-id: SECOND-SERVICE

server:
  port: 15000


spring:
  application:
    name: API-GATEWAY

